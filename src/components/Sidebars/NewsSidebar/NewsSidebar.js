import React from 'react'
import classes from './NewsSidebar.module.scss'

const NewsSidebar = () => {
    return (
        <div className={classes.NewsSidebar}>
        
        </div>
    )
}

export default NewsSidebar